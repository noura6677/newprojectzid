# CLAUDE.md — سياق مشروع «نجدية»

## نظرة عامة
متجر إلكتروني تجريبي (Demo Sandbox) باسم **نجدية** لقهوة مختصة.
خلف الواجهة يعمل محرك سلوك بالذكاء الاصطناعي (داخلياً اسمه «صانع الصفقات»)
لرفع متوسط السلة عبر اقتراحات + Gamification + توقيت.

> ⚠️ مهم: العميل (زائر المتجر) **يجب ألا يرى** أي ذكر للأداة أو أنها ديمو.
> الواجهة تخاطب الزائر كمساعد متجر عادي فقط.

## التقنية
موقع ثابت بدون أي تبعيات ولا خطوة بناء: `HTML / CSS / JS` فقط.
- `index.html` — الهيكل
- `styles.css` — التصميم (ثيم بنّي/كريمي، RTL عربي)
- `app.js` — محرك السلوك والحالة
- `data.js` — المنتجات والإعدادات (المؤقت/الجوائز/المستويات قابلة للتعديل هنا)

## السلوك المتفق عليه (مهم عند أي تعديل)
- **السلة:** مقفلة دائماً، تفتح فقط بزر "السلة"، وتنزلق من **اليسار** دون تغطية المنتجات.
- **فقاعة المساعد:** ثابتة دائماً (يمين، أسفل) مع حركة لطيفة، بدون أيقونة عقل.
- **شريط المستويات:** شريط **عمودي على اليمين** (مبتدئ→محترف) مع تعبئة تقدّم.
- **فتح المستويات:** لحظة احتفال وسط الشاشة (confetti) وليس إشعاراً عادياً.
- **بدون كشف الأداة:** لا شريط علوي، لا قسم شرح، ولا كلمة «صانع الصفقات» في الواجهة.

## التشغيل
```bash
python3 -m http.server 8000   # ثم http://localhost:8000
```

## النشر
GitHub Pages عبر workflow في `.github/workflows/pages.yml`
(الفرع `claude/demo-store-interface-9kGm5`، المصدر = GitHub Actions).
الرابط: https://noura6677.github.io/newprojectzid/
